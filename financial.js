const ctx = document.getElementById('myChart').getContext('2d');
const myChart = new Chart(ctx, {
    type: 'bar', // Change to other chart types as needed
    data: {
        // ... your chart data
    },
    options: {
        // ... chart options
    }
});
